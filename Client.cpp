//Includes
#include <sys/socket.h>
using namespace std;

int main(int argc, char* argv[]){
	//user passes in port/ or Ip
	if(argc != 2){
		printf("Enter a Port Number or IP address to indicate Server")
	}
	int input = argv[1];
	//determine if IP or Port.

	//call connect with input as IP or Port #
}

//Functions
	//Connect
		//Create Socket
		//Connect to Socket
		//Send and Recieve
	//Send
	//Recv
	//Encrypt
	//Decrypt

//unique username and password stored in server local.txt?